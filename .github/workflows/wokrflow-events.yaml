name:  Workflow events
on:
  push:
  pull_request:
  schedule:
    - cron: '*/5 * * * *' # Runs every 5minutes
workflow_dispatch:



jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hello, world!"